# QBC-001：备份后原地更新统计表

导入财务统计表时，默认继续使用用户提供的原文件路径，不生成要求用户改用的主副本。禁止直接覆盖：必须先在同目录临时文件中完成全部写入与校验，再生成与更新前原文件哈希一致的带时间戳备份，最后以原子替换方式更新原文件。替换后再次回读；若失败，立即从备份恢复。只有用户明确指定其他输出方式时，才偏离这一默认流程。

- 反例：导入后只交付「统计表_导入副本_时间戳.xlsx」，让用户自行切换主文件。
- 正例：保留原统计表路径作为主文件，并交付「写入前备份」路径作为回退点。
